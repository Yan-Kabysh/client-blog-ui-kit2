import './Category.scss';
import { CategoryProps } from './interfaces';
declare const Category: ({
  icon,
  title,
  text,
  type,
  onClick,
  className,
}: CategoryProps) => import('react/jsx-runtime').JSX.Element;
export default Category;
