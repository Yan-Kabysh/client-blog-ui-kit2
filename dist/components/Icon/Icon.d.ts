import { IconProps } from './interfaces';
import './index.scss';
declare const Icon: ({
  name,
  size,
  fill,
  link,
}: IconProps) => import('react/jsx-runtime').JSX.Element | null;
export default Icon;
